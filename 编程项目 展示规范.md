# 编程项目 展示规范

> Programming Project Showcase Specification
>
> 本规范定义「项目展示 md」的编写标准。任何编程项目只要按本规范在个人网站仓库的指定目录内提交一份（或一对）项目展示 md，网站构建时会自动读取并生成：**编程合集里的项目卡片** 与 **项目卡片对应的详情页**。

- 适用仓库：`https://github.com/HUliangwei/personal-website`
- 规范状态：V5 起生效
- 最后更新：2025

---

## 1. 目的与适用范围

本规范服务于「编程项目合集」（网站 `/projects` 页中的 `软件 / 编程` 区块）的内容扩展：

- **面向对象**：后续的编程项目（工具、脚本、Web 应用、机器学习项目等）。
- **核心约定**：项目内容以 Markdown（`.mdx`）文件形式存放在网站仓库的 `src/content/programming/` 目录内，由 Astro Content Collections 自动读取。
- **自动产物**：
  1. **项目卡片**：展示在 `/projects` 页编程合集展开区（标题、状态、摘要、技术栈、详情入口、GitHub 链接）。
  2. **详情页**：`/projects/programming/{slug}`（中文）与 `/en/projects/programming/{slug}`（英文），渲染 frontmatter 元信息 + 正文。
- **不适用**：研究型项目（SPAD 芯片、机器人、量子计算等）仍走 `src/content/projects/` 的老集合，不适用本规范。

> 网站侧的实现位于 `src/content.config.ts`（schema 校验）、`src/components/projects/ProgrammingCollection.astro`（卡片）、`src/components/pages/ProgrammingProjectPage.astro`（详情页）。规范与实现不一致时，以 `src/content.config.ts` 的 zod schema 为最终校验标准。

---

## 2. 工作机制总览

```text
编写项目展示 md
   │  中文文件：src/content/programming/{slug}.mdx
   │  英文文件：src/content/programming/{slug}.en.mdx
   ▼
Astro Content Collections 自动读取（glob: src/content/programming/**/*.{md,mdx}）
   │  校验 frontmatter（zod schema），不合法 → 构建失败
   ▼
/projects 页编程合集          /projects/programming/{slug}（及 /en 镜像）
   └─ 项目卡片                    └─ 项目详情页
      标题 / 状态 / 摘要           标题 / 状态 / 时间 / 技术栈 / 链接
      技术栈标签                   亮点（highlights）/ 封面（可选）
      查看详情 → 详情页            正文（mdx body，多级标题渲染）
      GitHub ↗ 外链
```

要点：

- **自动读取，无需改代码**：新增项目 = 新增 md 文件，不触碰任何 `.astro` / `.ts` 源码。
- **双语成对**：网站是中英双语，每个项目需要一份中文与一份英文文件（`{slug}.mdx` 与 `{slug}.en.mdx`），两份文件的 `slug` 必须相同。
- **slug 唯一**：`slug` 决定路由与卡片锚点，全站唯一，采用小写 kebab-case。

---

## 3. 目录与文件命名

| 项 | 规则 |
| --- | --- |
| 目录 | `src/content/programming/` |
| 中文文件 | `{slug}.mdx` |
| 英文文件 | `{slug}.en.mdx` |
| slug 格式 | 小写 kebab-case：`^[a-z0-9]+(?:-[a-z0-9]+)*$`，如 `videoto3d`、`personal-website`、`gaussian-splat-viewer` |
| 禁止 | 中文、大写、下划线、空格；不要与既有项目 slug 重复 |

示例：

```text
src/content/programming/
├── personal-website.mdx
├── personal-website.en.mdx
├── videoto3d.mdx
└── videoto3d.en.mdx
```

---

## 4. Frontmatter 字段规范

Frontmatter 位于文件开头的 `---` 与 `---` 之间，字段由 `src/content.config.ts` 中的 zod schema 校验。**字段名与类型必须与下表完全一致。**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `title` | string | ✅ | 项目名称（卡片与详情页标题）。中文文件用中文名，英文文件用英文名。 |
| `slug` | string | ✅ | 小写 kebab-case，两份语言文件必须相同。 |
| `locale` | `'zh' \| 'en'` | ✅ | `{slug}.mdx` 填 `zh`；`{slug}.en.mdx` 填 `en`。 |
| `status` | string | ✅ | 项目状态，推荐取值：`已完成` / `持续迭代` / `进行中`（英文 `Completed` / `Actively Iterated` / `In Progress`）。卡片与详情页顶部展示。 |
| `summary` | string | ✅ | 一句话摘要，≤ 120 字符。用于卡片描述与详情页引言，也是页面的 meta description。 |
| `technologies` | string[] | ✅ | 技术栈标签，如 `['Python', 'FFmpeg', 'COLMAP']`。卡片与详情页渲染为胶囊标签。 |
| `date` | string | ✅ | 时间范围，自由字符串但建议统一格式：`YYYY.MM–YYYY.MM` 或 `YYYY.MM–至今`（英文 `YYYY.MM–Present`）。影响合集内排序（新项目在前）。 |
| `featured` | boolean | ✅（有默认值） | `true` 置顶。合集内先按 `featured` 降序、再按 `date` 降序排列。 |
| `highlights` | string[] | 可选 | 项目亮点，最多 3 条。渲染在详情页摘要下方。 |
| `cover` | string | 可选 | 封面图路径（建议放 `public/` 下，如 `/models/xxx.png`），渲染在详情页头部。 |
| `links` | `{label, url}[]` | 可选 | 外部链接。约定至少一条 `label: 'GitHub'`、`url: 仓库地址` 的条目——卡片右下角会渲染为 `GitHub ↗`；其余条目渲染在详情页「链接」区。 |

### 校验规则（来自 zod schema）

```ts
schema: z.object({
  title: z.string(),
  slug: z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/, 'slug 必须为小写 kebab-case'),
  locale: z.enum(['zh', 'en']),
  status: z.string(),
  summary: z.string().max(120),
  technologies: z.array(z.string()),
  date: z.string(),
  featured: z.boolean().default(false),
  highlights: z.array(z.string()).max(3).optional(),
  cover: z.string().optional(),
  links: z.array(z.object({ label: z.string(), url: z.string().url() })).optional(),
})
```

> 常见失败原因：`url` 不是合法 URL（必须带 `https://`）、`summary` 超过 120 字符、`slug` 含大写或下划线、英文文件 `locale` 误填为 `zh`。
>
> **YAML 注意**：frontmatter 是 YAML，纯量值里出现「英文冒号 + 空格」（如 `Studio: FFmpeg`）会导致解析失败，请用引号包裹该字段，例如 `summary: "Local-first video-to-3D Studio: FFmpeg ..."`。中文全角冒号 `：` 不受影响。

---

## 5. 正文结构规范

正文（frontmatter 之后）是详情页的主体，用 `##` 二级标题分节，网站会把每节渲染为带样式的正文段落。建议按下列顺序组织，**可根据项目规模增删，但至少要有「项目简介」**：

| 建议章节（中文） | 对应英文标题 | 内容要点 |
| --- | --- | --- |
| `## 项目简介` | `## Overview` | 用 2–3 句话说明项目解决什么问题、产出什么。 |
| `## 功能特性` | `## Features` | 关键功能列表，用 `- **名称**：说明` 的条目式写法。 |
| `## 技术架构` | `## Architecture` | 技术选型、模块划分、数据流/管线。 |
| `## 关键实现` | `## Key Implementation` | 最能体现工程能力的实现细节。 |
| `## 挑战与解决` | `## Challenges & Decisions` | 遇到的问题与取舍，体现思考过程。 |
| `## 成果与展望` | `## Results & Next Steps` | 已达成的事实结果 + 后续计划。 |
| `## 链接` | `## Links` | 可再补充说明仓库/文档链接（可选，`links` frontmatter 已承担主要链接职责）。 |

写作约定：

- **如实陈述**：只写有证据支撑的内容；「已完成」与「进行中」通过 `status` 明确区分，不要夸大。
- **双语同步**：两份文件章节结构保持一致，内容各自用对应语言撰写，不要机翻堆砌。
- **格式**：支持标准 Markdown（多级标题、列表、`**加粗**`、`行内代码`、引用、链接）；暂不要求代码块、表格与图片（按需使用）。
- 注意：正文首行不要重复 frontmatter 中已有的 `title` / `summary`。

---

## 6. 完整模板

### 6.1 中文文件 `src/content/programming/{slug}.mdx`

```mdx
---
title: 项目中文名
slug: 项目-slug
locale: zh
status: 已完成
summary: 一句话摘要，不超过 120 字符，用于卡片与页面描述。
technologies:
  - Python
  - PyTorch
date: 2025.01–2025.06
featured: true
highlights:
  - 亮点一
  - 亮点二
cover: /projects/xxx.png
links:
  - label: GitHub
    url: https://github.com/你的账号/仓库名
---

## 项目简介

用 2–3 句话说明项目解决什么问题、产出什么。

## 功能特性

- **特性一**：说明。
- **特性二**：说明。

## 技术架构

说明技术选型、模块划分与数据流。

## 关键实现

写出最能体现工程能力的实现细节。

## 挑战与解决

遇到的问题与取舍。

## 成果与展望

已达成的事实结果，以及后续计划。
```

### 6.2 英文文件 `src/content/programming/{slug}.en.mdx`

```mdx
---
title: Project English Name
slug: 项目-slug
locale: en
status: Completed
summary: One-sentence summary under 120 characters for the card and page meta.
technologies:
  - Python
  - PyTorch
date: 2025.01–2025.06
featured: true
highlights:
  - Highlight one
  - Highlight two
cover: /projects/xxx.png
links:
  - label: GitHub
    url: https://github.com/你的账号/仓库名
---

## Overview

Two or three sentences on the problem this project solves and what it produces.

## Features

- **Feature one**: description.
- **Feature two**: description.

## Architecture

Technology choices, module breakdown, and data flow.

## Key Implementation

The implementation details that best show engineering ability.

## Challenges & Decisions

Problems encountered and trade-offs made.

## Results & Next Steps

Verified results so far, plus the roadmap.
```

> 模板只是起点：正文章节可按项目实际增删，frontmatter 字段（除 `title` / `slug` / `locale` / `status` / `summary` / `technologies` / `date` / `featured` 外）均可省略。

---

## 7. 新增 / 更新项目的操作步骤

1. **克隆/进入仓库**，确认在最新分支（当前主开发分支为 `feat/portfolio-v5`，发布分支为 `main`）。
2. **创建两份文件**：`src/content/programming/{slug}.mdx` 与 `{slug}.en.mdx`，按第 4、5、6 节填写。
3. **本地校验**（在仓库根目录执行）：
   ```powershell
   npm test        # 构建 + 回归测试（含内容 schema 校验）
   npm run build   # 确认静态构建通过
   ```
   任一失败说明 frontmatter 或内容不合法，按报错修改。
4. **预览**（可选）：`npm run dev`，打开 `/projects` 查看卡片、点进详情页核对渲染效果。
5. **提交推送**：
   ```powershell
   git add src/content/programming/
   git commit -m "feat: add {slug} programming showcase"
   git push
   ```
6. （可选）把项目仓库 README 的链接放进 `links`，保持双向可达。

---

## 8. 校验与发布

- **构建期强校验**：`npm run build` 会运行 zod schema 校验，任何不合规的展示 md 都会让构建失败——这保证了线上内容永远符合规范。
- **回归测试**：`tests/v5-programming.test.mjs` 锁定路由集合与关键产物（卡片链接、详情页标题/章节、GitHub 链接）；新增项目会改变路由集合，若测试硬编码了路由清单需同步更新。
- **发布**：构建产物由 Cloudflare Workers Static Assets 发布；规范文档与展示内容随仓库提交推送后自动进入下次部署。

---

## 9. 常见问题（FAQ）

**Q1：只写中文文件，不写英文文件可以吗？**
不可以。网站是双语站，英文路由 `/en/projects/programming/{slug}` 依赖英文文件；缺失会导致路由缺失与测试失败。

**Q2：`slug` 能改吗？**
能，但改动等于换路由，需要同步更新两份文件的 `slug`，并注意旧链接失效。

**Q3：卡片和详情页的排序规则是什么？**
合集内先 `featured: true` 置顶，其余按 `date` 字符串降序（新项目在前）。

**Q4：想放截图/演示图怎么办？**
把图片放进 `public/`（如 `public/projects/{slug}/cover.png`），在 frontmatter 的 `cover` 填 `/projects/{slug}/cover.png`；正文内也可用标准 Markdown 图片语法。

**Q5：研究型项目（芯片/机器人）也用这个规范吗？**
不用。研究型项目仍使用 `src/content/projects/` 与 `src/config/projects.ts` 的既有集合，本规范只覆盖编程项目合集。

**Q6：规范本身怎么维护？**
本文档即规范源。修改展示机制（schema、卡片、详情页、路由）后，请同步更新本文档与对应测试。
